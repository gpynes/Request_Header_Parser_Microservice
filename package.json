{
  "name": "request_header_parser_microservice",
  "version": "1.0.0",
  "description": "A simple microservice to retrieve metadata from request headers",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "restify": "^4.3.0"
  }
}
